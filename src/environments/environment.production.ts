// Configuración para ambiente de producción
export const environment = {
  production: true,
  apiUrl: 'https://backend-cursos-production.up.railway.app/api'  // Cambiar por la URL real de tu backend en producción
};
